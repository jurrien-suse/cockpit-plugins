[dosbox]
memsize=16
machine=svga_s3

[cpu]
cycles=fixed 15000  # Fixed cycles prevent erratic speed in some browsers

[ipx]
ipx=false           # Disable networking to prevent console errors

[autoexec]
mount c .
c:
# Ensure this matches your internal folder structure

DOOM.EXE
